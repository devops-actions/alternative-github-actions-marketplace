# Example environment file for local E2E testing.
# Copy this file to `.env` and adjust values to match your setup.
# Playwright will load `.env` automatically via the config.

# Frontend base URL (e.g., local Vite dev server or deployed SWA)
FRONTEND_BASE_URL=http://localhost:3000

# API base URL (e.g., local Azure Functions or deployed Function App)
API_BASE_URL=http://localhost:7071/api

# Number of Playwright parallel workers (optional, defaults to CPU count locally)
# PLAYWRIGHT_WORKERS=4
